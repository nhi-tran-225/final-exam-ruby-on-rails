<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Student List', root_path, class: "navbar-brand"%>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <%= link_to 'Item', list_items_path, class: "nav-link"%>
        </li>
        <li class="nav-item">
        <%= link_to 'Add Item', new_list_item_path, class: "nav-link"%>
        </li>
      </ul>
      <% if current_page? list_items_path %>
          <li class="nav-item d-flex align-items-center flex-row ms-auto me-3">
            <%= form_with(url: list_items_path, method: :get) do |form| %>
              <%= form.text_field :query, placeholder: "Item Search", class: "ps-2 pt-1 pb-1 me-2 rounded" %>
              <button type="submit" style="background: none; border: none; color: #ffffff ">
                <i class="fa fa-search"></i>
              </button>
            <% end %>
          </li>
        <% end %>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              
          <% if user_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete,turbo_confirm: 'Are you sure want to sign out?' } , class:'nav-link'%>
        </li>
        <%else%>
         <li class="nav-item">
          <%= link_to 'Sign in', new_user_session_path , class:'nav-link'%>
        </li>
        <li class="nav-item">
          <%= link_to 'Register', new_user_registration_path , class:'nav-link'%>
        </li>
        <%end%>
              
          </ul>
    </div>
  </div>
</nav>